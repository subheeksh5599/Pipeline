# Arc testnet RPC endpoint (from ARC CLI)
ARC_RPC_URL=http://localhost:8545

# Deployed PipelinePolicy contract address (from `./scripts/deploy.sh`)
POLICY_ADDRESS=

# Deployer / guardian private key (for write operations)
PRIVATE_KEY=

# Engine port
PORT=3100
