# Engine URL for the dashboard to call (default: local dev)
NEXT_PUBLIC_ENGINE_URL=http://localhost:3100

# Comma-separated list of agent addresses to track on the overview page
NEXT_PUBLIC_WATCHED_AGENTS=
